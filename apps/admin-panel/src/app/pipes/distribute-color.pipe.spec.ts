import { DistributeColorPipe } from './distribute-color.pipe';

describe('DistributeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DistributeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
